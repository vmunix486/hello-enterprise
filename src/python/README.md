# Python

The 2nd language added to the repo, and as of writing, the slowest. Here's a comparison of the different versions.

| Maker | Trick |
| --- | --- |
| vmunix | Decodes Base 64 -> Binary -> Hexadecimal -> Base 64 -> Hello, World! |
| Github Copilot | Generator, lots of classes |
| Claude | Basically same as the C version. This is the slowest one in the repo so far. |
